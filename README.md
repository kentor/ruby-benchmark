A collection of benchmarks for snippets of ruby code.

Clone and run, or run this in a shell if you trust the code:

`ruby -v; ruby -e "$(curl -fsSL https://raw.github.com/kentor/ruby-benchmark/master/FILENAME.rb)"`

